apiVersion: v1
kind: ServiceAccount
metadata:
  name: mock-mongodb
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: mock-mongodb
rules:
  - apiGroups: ["mongodb.com"]
    resources: ["mongodb"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["mongodb.com"]
    resources: ["mongodb/status"]
    verbs: ["get", "update", "patch"]
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: mock-mongodb
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: mock-mongodb
subjects:
  - kind: ServiceAccount
    name: mock-mongodb
    namespace: default
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-mongodb
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-mongodb
  template:
    metadata:
      labels:
        app: mock-mongodb
    spec:
      serviceAccountName: mock-mongodb
      containers:
        - name: controller
          image: ko://github.com/s-urbaniak/dbaas/cmd/mock-mongodb
          args:
            - --leader-elect=false
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
